export interface PlaygroundFile {
  name: string;
  content: string;
}
export interface Input {
  files: PlaygroundFile[];
  filesChange: (files: PlaygroundFile[]) => void;
}

split-panes
  @first
    editor files:=input.files
  @second
    iframe.result

style --
  iframe.result {
    width: 100%;
    height: 100%;
    border: none;
  }